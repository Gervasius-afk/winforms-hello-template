# Задание: Кнопка и приветствие

## Задача
При нажатии на кнопку **«Поздороваться»** в элементе `Label` должно появиться сообщение:

> **Привет, [ВАШЕ ИМЯ]!**

Замените `[ВАШЕ ИМЯ]` на **ваше настоящее имя** (как в зачётке).

## Требования
1. Обработчик нажатия кнопки должен быть подключён **в коде** (в конструкторе `Form1`) через:
   ```csharp
   btnGreet.Click += (s, e) => { ... };